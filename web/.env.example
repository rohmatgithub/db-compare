# Go API that /api/* is rewritten to. Read when next.config.ts loads, so a
# change needs a restart of `pnpm dev` (and a rebuild for `pnpm build`).
API_ORIGIN=http://localhost:8080

NEXT_TELEMETRY_DISABLED=1
